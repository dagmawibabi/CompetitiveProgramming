import random

def kthLargestNumber(nums, k):
    i = 0
    # for i in nums:
    #     for j in nums:

    # nums.sort()
    
    # chosen = nums[0]

    # chosen2 = 0
    # increments = k
    # print("Chosen 1: " + str(chosen))
    # for i in nums:
    #     if i != chosen and i > chosen:
    #         chosen2 = i
    # print("Chosen 2: " + str(chosen2))
    # while chosen2 != chosen and increments != 0:
    #     chosen = chosen + 1
    #     increments = increments - 1 
    # print(increments)

kthLargestNumber([2, 1, 4], 5)